import fs from "node:fs";import path from "node:path";const root=path.resolve(import.meta.dirname,"..");let files=0;let json=0;function walk(dir){for(const item of fs.readdirSync(dir,{withFileTypes:true})){if(["node_modules",".next","dist"].includes(item.name))continue;const p=path.join(dir,item.name);if(item.isDirectory())walk(p);else{files++;if(item.name.endsWith(".json")){JSON.parse(fs.readFileSync(p,"utf8"));json++;}}}}walk(root);console.log(`Source check OK: ${files} files, ${json} JSON files parsed.`);