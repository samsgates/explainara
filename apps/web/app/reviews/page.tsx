import { BrainCircuit, CalendarClock, CheckCircle2 } from "lucide-react";
import { Topbar } from "@/components/topbar";
const reviews=[{name:"TCP/IP Foundations",retention:61,due:"Due now",mins:3},{name:"Attention Masking",retention:66,due:"Today",mins:4},{name:"CAP theorem tradeoffs",retention:72,due:"Tomorrow",mins:5}];
export default function Reviews(){return <><Topbar title="Reviews" eyebrow="Memory engine"/><div className="grid grid-3"><div className="card"><span className="icon-box"><BrainCircuit size={18}/></span><div className="metric">3</div><b>Reviews due</b><p className="muted" style={{fontSize:12}}>Prioritized by predicted retention.</p></div><div className="card"><span className="icon-box"><CalendarClock size={18}/></span><div className="metric">12m</div><b>Total review time</b><p className="muted" style={{fontSize:12}}>Small retrieval checks, not full lessons.</p></div><div className="card"><span className="icon-box"><CheckCircle2 size={18}/></span><div className="metric">84%</div><b>30-day retention</b><p className="muted" style={{fontSize:12}}>Across reviewed concepts.</p></div></div><div className="section-head"><div><h2>Spaced review queue</h2><p className="muted">Explainara schedules a concept when predicted retention falls below target.</p></div></div><div className="card"><div className="list">{reviews.map(r=><div className="list-item" key={r.name}><div className="dot"/><div className="grow"><b>{r.name}</b><div className="muted" style={{fontSize:12}}>Predicted retention {r.retention}% · {r.mins} min</div></div><span className="tag warn">{r.due}</span><button className="button brand">Review</button></div>)}</div></div></>}
