import type { ReactNode } from "react";
import { AlertTriangle, BookOpen, BrainCircuit, Users } from "lucide-react";
import { Topbar } from "@/components/topbar";
const students=[{name:"Asha Raman",mastery:82,signal:"Ready to advance",status:"good"},{name:"Daniel Kim",mastery:64,signal:"Fragile retention",status:"warn"},{name:"Mira Patel",mastery:48,signal:"2 misconceptions",status:"bad"},{name:"Noah Chen",mastery:73,signal:"Application gap",status:"warn"}];
export default function Teacher(){return <><Topbar title="Teacher dashboard" eyebrow="Cohort intelligence"/><div className="grid grid-4"><Mini icon={<Users size={18}/>} v="42" l="Active learners"/><Mini icon={<BookOpen size={18}/>} v="6" l="Published courses"/><Mini icon={<BrainCircuit size={18}/>} v="71%" l="Avg. mastery"/><Mini icon={<AlertTriangle size={18}/>} v="7" l="Need attention"/></div><div className="section-head"><div><h2>Learners needing attention</h2><p className="muted">Prioritized from mastery, retention and misconception evidence.</p></div></div><div className="card"><table className="table"><thead><tr><th>Learner</th><th>Mastery</th><th>Signal</th><th>Recommended teacher action</th></tr></thead><tbody>{students.map(s=><tr key={s.name}><td><b>{s.name}</b></td><td>{s.mastery}%</td><td><span className={`tag ${s.status}`}>{s.signal}</span></td><td>{s.status==="bad"?"Review misconception evidence":s.status==="warn"?"Assign targeted practice":"No intervention"}</td></tr>)}</tbody></table></div><div className="grid grid-2" style={{marginTop:16}}><div className="card"><h2>Hardest concepts</h2>{[["NAT and Routing",58],["Attention Masking",62],["CAP theorem tradeoffs",66],["Kubernetes Services",69]].map(([n,v])=><div style={{marginBottom:14}} key={n as string}><div style={{display:"flex",justifyContent:"space-between",fontSize:13,marginBottom:5}}><span>{n}</span><b>{v}% cohort mastery</b></div><div className="progress"><span style={{width:`${v}%`}}/></div></div>)}</div><div className="card"><h2>Common misconceptions</h2><div className="list"><div className="list-item"><div className="grow"><b>NAT modifies application payload</b><div className="muted" style={{fontSize:12}}>7 learners · Kubernetes Networking</div></div><span className="tag bad">high</span></div><div className="list-item"><div className="grow"><b>Attention is a fixed token lookup</b><div className="muted" style={{fontSize:12}}>5 learners · Transformers</div></div><span className="tag warn">medium</span></div></div></div></div></>}
function Mini({icon,v,l}:{icon:ReactNode;v:string;l:string}){return <div className="card"><span className="icon-box">{icon}</span><div className="metric">{v}</div><b style={{fontSize:13}}>{l}</b></div>}
